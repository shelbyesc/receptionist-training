services:
  - type: web
    name: receptionist-training
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
      - key: DEFAULT_TENANT
        value: default
